// Estrapolo il metodo createApp
const { createApp } = Vue;

// Inizializzo l'app Vue
const app = createApp({
    data() {
        return {
            
        }
    }
});

// La monto nell'elemento HTML "root"
app.mount('#root');
